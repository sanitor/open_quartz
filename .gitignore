# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
coverage
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Regenerated by `npm run copy:ort` — onnxruntime-web runtime files.
public/ort/

# Regenerated by `npm run build:wasm` (wasm-pack output).
rust/crates/*/pkg/*
!rust/crates/*/pkg/yolo_detector.js
!rust/crates/*/pkg/yolo_detector.d.ts
!rust/crates/*/pkg/snippets/
!rust/crates/*/pkg/snippets/**
rust/**/target/
# Track Cargo.lock: git deps (rimeflow-yolov8n) must be pinned so
# `build:wasm` produces the same inline_js bridge that's committed in pkg/.
# rust/**/Cargo.lock

# Regenerated by `npm run copy:model` — pulled from upstream rimeflow-yolov8n
# git checkout that Cargo materializes via `npm run build:wasm`.
public/models/
.vitest-attachments/
test-results/
tests/shaders/__screenshots__/
